services:
  spring-boot-service:
    container_name: spring-boot-container
    build:
      context: .. # 이미지 빌드 컨텍스트를 demo 디렉토리로 설정
      dockerfile: docker/Dockerfile  # 이미지 빌드를 위한 Dockerfile 경로 지정
    ports:
      - 8080:8080